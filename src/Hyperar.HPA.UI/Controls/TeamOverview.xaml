<UserControl x:Class="Hyperar.HPA.UI.Controls.TeamOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Hyperar.HPA.UI.ViewModels"
             xmlns:globalization="clr-namespace:Hyperar.HPA.Globalization;assembly=Hyperar.HPA.Globalization"
             xmlns:converters="clr-namespace:Hyperar.HPA.UI.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:HomeViewModel}">
    <UserControl.Resources>
        <converters:BookingStatusToSmallBitmapImageConverter x:Key="bookingSmallStatusToBitmapImageConverter" />
        <converters:ByteArrayToBitmapImageConverter x:Key="byteToImageConverter" />
        <converters:DateToLocaleShortDateTimeString x:Key="dateToLocaleDateTimeString" />
        <converters:HasMotherClubBonusToSmallBitmapImageConverter x:Key="hasMotherClubBonusToSmallBitmapImageConverter" />
        <converters:HealthToSmallBitmapImageConverter x:Key="healthToSmallBitmapImageConverter" />
        <converters:HealthToWeekStringConverter x:Key="healthToWeekStringConverter" />
        <converters:IsTransferListedToSmallBitmapImageConverter x:Key="isTransferListedToSmallBitmapImageConverter" />
        <converters:SpecialtyToSmallBitmapImageConverter x:Key="specialtyToSmallBitmapImageConverter" />
        <converters:SupporterTierToTranslatedString x:Key="supporterTierToTranslatedString" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Senior Team -->
        <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Vertical" Margin="25 0">
            <TextBlock Style="{StaticResource BlockTitle}" Text="Team" />

            <Image Height="210" Width="260" Source="{Binding TeamOverview.SeniorTeam.Logo, FallbackValue={StaticResource no-logo}, TargetNullValue={StaticResource no-logo}, Converter={StaticResource byteToImageConverter}}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0" Text="{x:Static globalization:Strings.Team}" />
                <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding TeamOverview.SeniorTeam}" />

                <TextBlock Grid.Column="0" Grid.Row="1" Text="{x:Static globalization:Strings.Country}" />
                <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding TeamOverview.Country}" />

                <TextBlock Grid.Column="0" Grid.Row="2" Text="{x:Static globalization:Strings.Region}" />
                <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding TeamOverview.Region}" />

                <TextBlock Grid.Column="0" Grid.Row="3" Text="{x:Static globalization:Strings.League}" />
                <TextBlock Grid.Column="1" Grid.Row="3" Text="{Binding TeamOverview.SeniorSeries}" />
            </Grid>
        </StackPanel>

        <!-- Manager -->
        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Vertical" Margin="25 0">
            <TextBlock Style="{StaticResource BlockTitle}" Text="Manager" />
            <Image Height="123" Width="92" Source="{Binding TeamOverview.Manager.Avatar, FallbackValue={StaticResource sil1_3}, TargetNullValue={StaticResource sil1_3}, Converter={StaticResource byteToImageConverter}}" />

            <TextBlock Text="{Binding TeamOverview.Manager}" />

            <TextBlock Text="{Binding TeamOverview.Manager.SupporterTier, Converter={StaticResource supporterTierToTranslatedString}}" />
        </StackPanel>

        <!-- Played Matches -->
        <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Vertical" MinHeight="250" Margin="25 0">
            <TextBlock Style="{StaticResource BlockTitle}" Text="Recent Matches" />

            <ItemsControl  ItemsSource="{Binding TeamOverview.PlayedMatches, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding HomeTeam.Name}" Width="250" FontSize="14" TextAlignment="Center" />
                            <TextBlock Text="{Binding HomeGoals}" Width="20" FontSize="14" FontWeight="Bold" TextAlignment="Center" />
                            <TextBlock Text="-" Width="20" FontWeight="Bold" FontSize="14" TextAlignment="Center" />
                            <TextBlock Text="{Binding AwayGoals}" Width="20" FontSize="14" FontWeight="Bold" TextAlignment="Center" />
                            <TextBlock Text="{Binding AwayTeam.Name}" Width="250" FontSize="14" TextAlignment="Center" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Upcoming Matches -->
        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Vertical" MinHeight="250" Margin="25 0">
            <TextBlock Style="{StaticResource BlockTitle}" Text="Upcoming Matches" />

            <ItemsControl ItemsSource="{Binding TeamOverview.UpcomingMatches, Mode=OneWay}" HorizontalAlignment="Center">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Date, Converter={StaticResource dateToLocaleDateTimeString}}" FontSize="14" Width="120" />
                            <TextBlock Text="{Binding HomeTeam.Name}" Width="250" FontSize="14" TextAlignment="Center" />
                            <TextBlock Text="-" Width="20" FontWeight="Bold" FontSize="14" TextAlignment="Center" />
                            <TextBlock Text="{Binding AwayTeam.Name}" Width="250" FontSize="14" TextAlignment="Center" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Players -->
        <StackPanel Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" Orientation="Vertical" MinHeight="250" Margin="25 0">
            <TextBlock Style="{StaticResource BlockTitle}" Text="Players" />

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <DataGrid AlternatingRowBackground="#fff"
                      AutoGenerateColumns="False"
                      Background="#fff"
                      BorderThickness="0"
                      GridLinesVisibility="None"
                      HeadersVisibility="None"
                      IsReadOnly="True"
                      ItemsSource="{Binding TeamOverview.SeniorPlayers, Mode=OneWay}"
                      RowBackground="#fff"
                      SelectionUnit="FullRow">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="IsHitTestVisible" Value="False" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="FontSize" Value="16" />
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding FirstName}" />
                        <DataGridTextColumn Binding="{Binding NickName}" />
                        <DataGridTextColumn Binding="{Binding LastName}" />
                        <DataGridTemplateColumn Width="SizeToCells">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Stretch="None" Margin="5 0" Source="{Binding BookingStatus, FallbackValue={StaticResource transparent}, TargetNullValue={StaticResource transparent}, Converter={StaticResource bookingSmallStatusToBitmapImageConverter}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="SizeToCells">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Stretch="None" Margin="5 0" Source="{Binding IsTransferListed, FallbackValue={StaticResource transparent}, TargetNullValue={StaticResource transparent}, Converter={StaticResource isTransferListedToSmallBitmapImageConverter}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="SizeToCells">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Stretch="None" Margin="5 0" Source="{Binding HealthStatus, FallbackValue={StaticResource transparent}, TargetNullValue={StaticResource transparent}, Converter={StaticResource healthToSmallBitmapImageConverter}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="SizeToCells">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding HealthStatus, Converter={StaticResource healthToWeekStringConverter}}" FontWeight="Bold" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="SizeToCells">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Stretch="None" Margin="5 0" Source="{Binding HasMotherClubBonus, FallbackValue={StaticResource transparent}, TargetNullValue={StaticResource transparent}, Converter={StaticResource hasMotherClubBonusToSmallBitmapImageConverter}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="SizeToCells">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Stretch="None" Margin="5 0" Source="{Binding Specialty, FallbackValue={StaticResource transparent}, TargetNullValue={StaticResource transparent}, Converter={StaticResource specialtyToSmallBitmapImageConverter}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>