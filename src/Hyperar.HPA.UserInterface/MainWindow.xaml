<Window x:Class="Hyperar.HPA.UserInterface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hyperar.HPA.UserInterface"
        xmlns:controls="clr-namespace:Hyperar.HPA.UserInterface.Controls"
        xmlns:viewModels="clr-namespace:Hyperar.HPA.UserInterface.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
        WindowState="Maximized"
        mc:Ignorable="d"
        MinHeight="768"
        MinWidth="1024">
    <Window.Background >
        <ImageBrush
            ImageSource="Resources/BackgroundDark.jpg" 
            TileMode="Tile" 
            Stretch="Uniform"
            AlignmentY="Top"
            Viewport="0,0,292,292"
            ViewportUnits="Absolute" />
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:NavigationBar Grid.Row="0" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ContentControl Content="{Binding CurrentViewModel}"/>
        </ScrollViewer>
    </Grid>
</Window>
